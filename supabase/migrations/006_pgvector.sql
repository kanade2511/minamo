-- Minamo (水面) — pgvector Extension (duplicate tracking)
-- This migration is a no-op; it was created to re-run pgvector setup.
-- The extension and column are already applied in 005_pgvector.

SELECT 1;
